# 2026-06-16 工作日志

## 盘后数据更新（自动化 21:00）
- update-daily.js 首次运行失败：第617/624行引用了第635行才声明的 `const wp` 变量（TDZ错误）
- 修复方案：在第607行之前用 `updateData.weipan` 提前取最新微盘日期（`_wpLatestDate`），替换两处对 `wp` 的提前引用
- 修复后脚本成功运行，auto-data.js 更新（143KB），数据日期 2026-06-15
- 评分结果：总分42分 → 回避区，硬风控 watch（未触发）
- GitHub push 连续3次 Connection Reset，网络问题，本地 commit a3aeb00 待手动推送
- allAMedian 5日序列查询失败，用单日值1.24兜底（非致命警告）

### 教训
- update-daily.js 的"全A中位数5日序列"区块（第606-629行）在数据写入段（第635行）之前引用了 `wp`，这是 JS temporal dead zone 问题。已在本次修复，后续不会再出现。

## 第二轮修复（05:25）— ma243均线为null
- 用户反馈"微盘相对科创50站上243日均线"数据为空
- 根因：K线回溯仅260自然日（约168交易日），不够243日均线所需数据量
- 修复：将回溯天数从260改为400自然日，拿到267个交易日K线
- 结果：ma243=40239.65，ma243Position=-23.10%（相对净值远低于243日均线）
- auto-data.js 从143KB增至220.7KB
- 本地 commit 7ad16d1，GitHub push 又遇 Connection Reset，待网络恢复手动推送

## 13:00 自动化盘后数据更新（automation-1781496849510）

- 运行 update-daily.js，成功拉取全部17项数据
- 数据日期 2026-06-16（实时交易日数据）
- 评分：总分35分，回避区（gate=watch）
- 微盘 close=540731.48, chg=-0.05%, upCount=3902, limitDown=12, repairDays=2
- allAMedian 批量查询失败，用单日值0兜底（⚠️非致命）
- 成功推送到 GitHub（beb71c0）
- scoring.js 无 module.exports，是浏览器全局对象 ScoringEngine，需在 vm 中一起加载
